# This project does not require environment variables for normal operation.
# Configure Nacos connection metadata in DSH Settings and its access token through
# the DSH credential service; do not store secrets in a .env file.
